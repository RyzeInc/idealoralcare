# URL Registry — Ideal Oral Health

Every URL the platform serves, what it is for, and who can reach it. Production base URL: `https://getidealoh.com`.

> Generated by `npm run docs:urls` from `src/lib/site-routes.ts` and `src/lib/admin-nav.ts` — the same definitions that produce `sitemap.xml`, `robots.txt`, and the admin sidebar. Editing the output by hand will be overwritten.

## Access levels

| Level | Meaning |
| --- | --- |
| Public | No account required. |
| Member | Requires a signed-in member with an active membership. |
| Partner | Requires a distribution partner with a resolved scope; each partner sees only their own book. |
| Staff | Requires an internal `adminUsers` record. |
| Owner | Requires the `owner` role specifically. |
| Signed webhook | Machine-to-machine. Unauthenticated by design, verified by provider signature. |

Access is enforced server-side on every route *and* independently inside each Convex function. Hiding a link is never the control.

## Worth knowing

- **`/register` and `/register/partnerkit` are named backwards.** `/register` is the *signing* flow (Partner Agreement + W-9); `/register/partnerkit` is the *sign-up form* that requests the kit. Worth renaming before either URL goes on printed collateral.
- **`/health/plans/:slug` still serves hardcoded sample data** and is not linked from the plans page. It is deliberately excluded from the sitemap and blocked in robots.txt until it reads the live catalogue.
- **`/bootstrap`, `/debug/email-test`, and `/debug/employer-membership-agreement` are development utilities** that are crawl-blocked but not access-gated. Anyone with the URL can load them.
- **Any unreserved single-segment path is a rep vanity URL.** Adding a new top-level route means adding it to `src/lib/rep-routing/reserved.ts` as well, or a rep code can shadow it.
- **White-label brand sites are excluded from our sitemap on purpose** — they are near-duplicates of `/health` and would compete with the primary brand for the same search queries. They stay crawlable, so partners keep any organic traffic they earn.

## Marketing

Public pages. These are the indexable surface — everything here is in `sitemap.xml` unless a note says otherwise.

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/` | Plan chooser: routes visitors to the Oral Savings Plans (/health) or the Essentials membership (/newideal).<br><em>Excluded from the sitemap so /health stays the single canonical entry point for search.</em> | Public | — |
| `/health` | Primary brand landing page: hero, value props, plan entry points. | Public | Yes |
| `/health/plans` | Plan catalogue with pricing and cadence selection. Accepts ?ref= for rep attribution. | Public | Yes |
| `/health/plans/:slug` | Long-form detail for a single plan: inclusions, exclusions, FAQ.<br><em>Held back from the sitemap: still served from hardcoded MOCK_PRODUCTS and not linked from /health/plans. Add to the sitemap once it reads the live catalogue.</em> | Public | — |
| `/health/compare` | Side-by-side plan comparison table. | Public | Yes |
| `/health/how-it-works` | Explains the membership model end to end. | Public | Yes |
| `/health/discount` | Careington network savings — the discount side of the product. | Public | Yes |
| `/health/teledentistry` | 24/7 virtual dental consults. | Public | Yes |
| `/health/oral-health-scan` | Toothlens SmileScan explainer and entry point. | Public | Yes |
| `/newideal` | Landing page for the Ideal Health Essentials membership (telehealth, Rx, labs, mental health) alongside Oral Care.<br><em>The /newideal pages were never in the sitemap; add them here once Essentials is ready to be indexed.</em> | Public | — |
| `/newideal/plans` | Plan chooser for Essentials and Oral Care. Default landing page for rep vanity URLs (/{code}). | Public | — |
| `/newideal/essentials` | Essentials membership detail and the four coverage tiers. Rep links land here with ?to=essentials. | Public | — |
| `/newideal/oralcare` | Oral Care plan detail in the Essentials-branded chrome. Rep links land here with ?to=oralcare. | Public | — |
| `/health/dental` | Dental-specific landing page for paid and organic search. | Public | Yes |
| `/health/faq` | Common member questions before purchase. | Public | Yes |
| `/health/shop` | Affiliate storefront for preventative-care products. Outbound links, not plans.<br><em>404s when the global shop kill switch is off (shop.queries.isEnabled). Listed anyway — the switch is an operational pause, not a decision to deindex.</em> | Public | Yes |
| `/health/blog` | Editorial and SEO content hub. | Public | Yes |
| `/health/blog/:slug` | Individual article. Enumerated in the sitemap from BLOG_POSTS.<br><em>Expanded per post by src/app/sitemap.ts; each post's own publish date is its lastModified.</em> | Public | — |
| `/health/enroll` | Guided enrollment: flow selection, broker attribution, member details. | Public | Yes |
| `/health/terms` | Member terms. | Public | Yes |
| `/health/privacy` | Privacy notice. | Public | Yes |

## Member portal

Reachable without a link but never indexed. Sign-in, checkout, and the member's own account pages.

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/newideal/checkout` | Stripe checkout for Essentials and Oral Care, with the signed membership agreement persisted at signing.<br><em>Intentionally public — authentication happens inline, as on /health/checkout.</em> | Public | — |
| `/newideal/success` | Post-payment confirmation for the /newideal checkout. | Public | — |
| `/health/sign-in` | Clerk-backed sign-in, hosted on our domain rather than Clerk's. | Public | — |
| `/health/sign-up` | Account creation. | Public | — |
| `/health/forgot-password` | Password reset request. | Public | — |
| `/health/sso-callback` | OAuth return URL. Never linked directly. | Public | — |
| `/health/claim-invite` | Redeems an emailed invite for a group-enrolled employee.<br><em>Tokenised link sent by email; the token is the gate, not a session.</em> | Public | — |
| `/health/checkout` | Stripe checkout for a selected plan and cadence.<br><em>Intentionally public — the Account step authenticates inline so a visitor never loses their cart to a sign-in redirect.</em> | Public | — |
| `/health/dashboard` | Membership status, ID card, scan history, benefits. | Member | — |
| `/health/manage-plans` | Change, add, or cancel plans; open the Stripe billing portal. | Member | — |
| `/health/admin-redirect` | Lands a freshly signed-up user on the right portal (member, partner, or admin).<br><em>Transient — calls /api/admin/post-signup-check and forwards.</em> | Member | — |

## Registration

Public lead capture. These feed the Partner Kit Leads and Partner Applications queues in the admin console.

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/register` | Sign the Partner Agreement and W-9, or upload completed forms.<br><em>Naming is inverted against /register/partnerkit: this is the signing flow, that one is the sign-up form. Worth renaming before either URL is printed on collateral.</em> | Public | Yes |
| `/register/partnerkit` | Agency and business registration form requesting the partner kit.<br><em>Feeds Admin → Partner Kit Leads.</em> | Public | Yes |
| `/register/rep` | Onboarding submission for brokers, agencies, and front-line reps.<br><em>Feeds Admin → Partner Applications.</em> | Public | Yes |
| `/:repSlug` | Any unreserved single-segment path resolves against the rep-code table, records the visit, and redirects to /health/plans?ref=CODE with a 90-day attribution cookie.<br><em>Handled in src/proxy.ts, ahead of auth. Reserved segments are listed in src/lib/rep-routing/reserved.ts — adding a top-level route means adding it there too, or a rep slug can shadow it.</em> | Public | — |

## White-label

Partner-branded mirrors of the marketing and member pages, themed per site from the `sites` table.

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/:siteSlug` | Partner-branded mirror of the marketing and member pages, themed per site from the Convex `sites` table.<br><em>Mirrors: /, /plans, /compare, /how-it-works, /discount, /teledentistry, /oral-health-scan, /dental, /faq, /shop, /blog, /blog/:slug, /enroll, /checkout, /dashboard, /manage-plans, /claim-invite, /sign-in, /sign-up, /forgot-password, /sso-callback, /terms, /privacy. Deliberately absent from our sitemap — near-duplicates of /health would compete with the primary brand. Left crawlable so partners keep any organic traffic they earn.</em> | Public | — |

## Partner portal

Distribution partners see their own book here, and only their own book. Partners have no CRM access at all — that boundary is enforced per Convex function, not just per route.

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/partner` | Book-of-business summary for the signed-in partner. | Partner | — |
| `/partner/members` | Every member the partner is credited for. | Partner | — |
| `/partner/production` | Funnel, lead sources, and follow-ups. | Partner | — |
| `/partner/retention` | Cohorts, churn, and revenue movement. | Partner | — |
| `/partner/downline` | Sub-agencies and reps beneath this partner.<br><em>Hidden for reps, who have no downline.</em> | Partner | — |
| `/partner/groups` | Employer accounts and participation rates. | Partner | — |
| `/partner/watchlist` | Accounts and members needing action today. | Partner | — |
| `/partner/resources` | Marketing material, partner kit, collateral, and forms. | Partner | — |

## Admin console

Internal staff only. Ordered below as the sidebar orders it.

### Overview

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/admin` | Daily snapshot: alerts, activity feed, system health, quick links. | Staff | — |
| `/admin/insights` | Whole-book performance: growth, revenue, retention, data health. | Staff | — |

### Members & Partners

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/admin/members` | Every person in a plan, at any lifecycle stage. | Staff | — |
| `/admin/brokers` | Program managers, FMOs, and agencies reselling the plans. | Staff | — |
| `/admin/rep-codes` | Attribution codes and the vanity URLs that resolve to them. | Staff | — |
| `/admin/partnerkit` | Inbound registrations from /register/partnerkit. | Staff | — |
| `/admin/partner-applications` | Review broker, agency, and rep onboarding submissions from /register/rep. | Staff | — |
| `/admin/resources` | Manage the collateral that partners download. | Staff | — |

### Operations

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/admin/hierarchy` | Sites → accounts → organizations. Also where a site's branding, domain, and integrations are edited. | Staff | — |
| `/admin/eligibility` | Upload and reconcile employer member rosters. | Staff | — |
| `/admin/vendor-files` | Generate and download outbound vendor files for manual delivery. | Staff | — |

### Finance

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/admin/billing` | Subscription billing and group invoices. | Staff | — |
| `/admin/list-bill` | Payroll-deduction groups and monthly employer remittance. | Staff | — |
| `/admin/list-bill-invoices` | Group-paid roster invoices. | Staff | — |
| `/admin/vendor-statements` | Locked monthly remittance statements for Toothlens, Careington, Ideal, and Ryze. | Staff | — |
| `/admin/invoice-calculator` | Per-member revenue and dispersal breakdown by group and partner. | Staff | — |
| `/admin/commissions` | Agent commission reporting.<br><em>Coming soon — figures are not yet reliable.</em> | Staff | — |
| `/admin/shop` | Affiliate products, ordering, and the storefront kill switch. | Staff | — |

### Support

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/admin/customer-service` | Member lookup and issue resolution. | Staff | — |
| `/admin/communications` | Send or re-send member email one at a time or en masse, with the full send log. | Staff | — |

### System

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/admin/users` | Invite teammates and manage admin access. | Staff | — |
| `/admin/user-audit` | Reconcile one person across Clerk, Convex, and Toothlens; repair or delete identity records. | Staff | — |
| `/admin/audit-log` | Append-only, system-wide trail of admin actions. | Staff | — |
| `/admin/settings` | Global site text: name, contact details, social links.<br><em>Branding, domain, and integrations live under Hierarchy → Edit Site, not here.</em> | Staff | — |
| `/admin/dev-tools` | Developer and maintenance utilities. | Owner | — |

### Help

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/admin/docs/sops` | Step-by-step procedures for recurring admin tasks. | Staff | — |
| `/admin/docs/guide` | Narrative operator guide to the console. | Staff | — |
| `/admin/help` | Terminology and common workflows for new operators. | Staff | — |

### Detail pages (reached from the pages above, not the sidebar)

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/admin/members/:id` | Single member: subscription, dependents, scans, billing, audit trail. | Staff | — |
| `/admin/list-bill-invoices/:groupId` | Invoice history for one list-bill group. | Staff | — |
| `/admin/list-bill-invoices/invoice/:invoiceId` | Single list-bill invoice with line items and payment status. | Staff | — |
| `/admin/vendor-statements/:statementId` | One locked statement with its document and line detail. | Staff | — |
| `/admin/vendor-statements/activity` | Cross-vendor history of generation, locking, and delivery. | Staff | — |
| `/admin/vendor-statements/disclosure` | Controls which columns each vendor sees on their statement. | Staff | — |
| `/admin/docs/:section/:slug` | A single SOP or guide chapter, compiled from docs/admin/**. | Staff | — |

## CRM

Internal staff only, gated a second time on top of the admin gate: some CRM contacts are themselves brokers.

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/admin/crm` | Pipeline summary and today's work.<br><em>The whole /admin/crm tree is gated twice — once in the route layout and once per Convex function — because some CRM contacts are themselves brokers. Distribution partners never reach it.</em> | Staff | — |
| `/admin/crm/deals` | Deal board by stage. | Staff | — |
| `/admin/crm/deals/:id` | One deal: stage history, contacts, activity. | Staff | — |
| `/admin/crm/contacts` | Prospect and customer contacts. | Staff | — |
| `/admin/crm/contacts/:id` | One contact: touch history, emails, calls, notes. | Staff | — |
| `/admin/crm/companies` | Prospect organizations and their deal stage. | Staff | — |
| `/admin/crm/companies/:id` | One company: people, deals, activity. | Staff | — |
| `/admin/crm/tags` | Tag taxonomy for contacts and companies. | Staff | — |
| `/admin/crm/segments` | Saved filters used to target campaigns. | Staff | — |
| `/admin/crm/workflows` | Automation rules and sequences. | Staff | — |
| `/admin/crm/workflows/:id` | One workflow's steps and run history. | Staff | — |
| `/admin/crm/campaigns` | Outbound email campaigns. | Staff | — |
| `/admin/crm/campaigns/new` | Compose and schedule a campaign. | Staff | — |
| `/admin/crm/campaigns/:id` | One campaign: recipients, sends, engagement. | Staff | — |
| `/admin/crm/drip` | Multi-phase outreach tracks and who is enrolled on each.<br><em>A drip campaign is the track (e.g. five broker emails); a campaign under /admin/crm/campaigns is one blast within it. Enrollment, not the blast's recipient list, is what records who is on a track and at which phase.</em> | Staff | — |
| `/admin/crm/drip/:id` | One track: phase breakdown, members, and bulk phase moves. | Staff | — |
| `/admin/crm/import` | CSV import for contacts and companies. | Staff | — |
| `/admin/crm/inbox` | Inbound replies and unhandled conversations. | Staff | — |
| `/admin/crm/analytics` | Pipeline conversion and outbound performance. | Staff | — |
| `/admin/crm/settings` | Pipeline stages, sending identities, and compliance settings. | Staff | — |

## System

Operational and development endpoints.

| Path | Purpose | Access | Sitemap |
| --- | --- | --- | --- |
| `/unsubscribe/:token` | One-click unsubscribe for CRM email recipients.<br><em>Public by necessity — the recipient has no account. The token is the only gate. Must stay reachable: CAN-SPAM requires it.</em> | Public | — |
| `/bootstrap` | One-time setup that grants the first signed-in user admin access.<br><em>Setup utility. Should be removed or hard-gated once the first admin exists in an environment.</em> | Member | — |
| `/debug/email-test` | Renders and sends transactional email templates against a test address.<br><em>Development aid. Blocked in robots.txt; not otherwise gated.</em> | Public | — |
| `/debug` | Index of the development-only preview and test harnesses.<br><em>Development aid. Blocked in robots.txt; not otherwise gated.</em> | Public | — |
| `/debug/email-preview` | Renders every template in the email registry from fixture data, without sending.<br><em>Development aid. Blocked in robots.txt; not otherwise gated.</em> | Public | — |
| `/debug/pdf-preview` | Renders any PDF the system can generate from fixture data — no real member or invoice needed.<br><em>Development aid. Blocked in robots.txt; not otherwise gated.</em> | Public | — |
| `/debug/employer-membership-agreement` | Renders the employer membership agreement PDF for proofreading.<br><em>Development aid. Blocked in robots.txt; not otherwise gated.</em> | Public | — |
| `/sitemap.xml` | Generated from this registry by src/app/sitemap.ts. | Public | — |
| `/robots.txt` | Generated from ROBOTS_DISALLOW by src/app/robots.ts. | Public | — |

## API

HTTP endpoints, grouped by integration. Every one of these is blocked in `robots.txt`; the access column is what the endpoint itself enforces.

### Stripe — `/api/stripe`

Checkout, subscription changes, refunds, and the billing portal. **Access:** Member.

```
POST /api/stripe/checkout
POST /api/stripe/change-plan
POST /api/stripe/cancel
POST /api/stripe/setup-payment
POST /api/stripe/billing-portal
GET  /api/stripe/member-invoices
POST /api/stripe/sync
POST /api/stripe/admin-cancel  (staff)
POST /api/stripe/admin-refund  (staff)
POST /api/stripe/webhook       (unauthenticated by design — Stripe-signed)
```

### Clerk identity — `/api/clerk`

User directory reads and identity webhook intake. **Access:** Staff.

```
GET    /api/clerk/users
GET    /api/clerk/users/:id
DELETE /api/clerk/users/:id
POST   /api/clerk/webhook  (unauthenticated by design — Svix-signed)
```

### Admin operations — `/api/admin`

Server-side admin utilities: PDF generation, invite lookup, vendor delivery. **Access:** Staff.

```
GET  /api/admin/get-invite-by-email
GET  /api/admin/post-signup-check
GET  /api/admin/members/:memberId/id-card
GET  /api/admin/list-bill-invoices/:invoiceId/group-pdf
GET  /api/admin/list-bill-invoices/preview-sample
POST /api/admin/vendor-deliver
GET  /api/admin/vendor-statements/:statementId/document
GET  /api/admin/vendor-statements/period/:period/document
```

### CRM public endpoints — `/api/crm`

Endpoints reachable by email recipients, who have no account. **Access:** Public.

```
GET /api/crm/unsubscribe
```

### Twilio voice — `/api/twilio`

Click-to-call from the CRM: token minting, call control, recordings. **Access:** Staff.

```
GET  /api/twilio/token
POST /api/twilio/voice      (Twilio-signed)
POST /api/twilio/status     (Twilio-signed)
POST /api/twilio/recording  (Twilio-signed)
```

### Documents & PDFs — `/api/documents`

Generates member cards, agreements, W-9s, and fulfillment paperwork. **Access:** Member.

```
GET/POST /api/documents
POST     /api/member-card-pdf
POST     /api/generate-partner-agreement-pdf
POST     /api/generate-w9-pdf
POST     /api/generate-fulfillment-pdf
```

### Toothlens — `/api/toothlens`

Scan completion callback from the SmileScan provider. **Access:** Signed webhook.

```
POST /api/toothlens/scan-completed
```

### Resend — `/api/resend`

Email delivery, open, click, and bounce events. **Access:** Signed webhook.

```
POST /api/resend/webhook
```

### Shop — `/api/shop`

Affiliate outbound click tracking. **Access:** Public.

```
POST /api/shop/click
```

### Attribution — `/api/track`

Records rep-link visits for attribution reporting. **Access:** Public.

```
POST /api/track/rep-visit
```

### Email test — `/api/test-email`

Development-only send used by /debug/email-test. **Access:** Public.

```
POST /api/test-email
```

## Search engine surface

`sitemap.xml` lists **17 static pages** plus one entry per blog post. Blog posts are expanded from `src/app/health/blog/posts.ts`, each carrying its own publish date.

`robots.txt` disallows:

```
Disallow: /admin
Disallow: /partner
Disallow: /api
Disallow: /debug
Disallow: /bootstrap
Disallow: /unsubscribe
Disallow: /health/dashboard
Disallow: /health/manage-plans
Disallow: /health/checkout
Disallow: /health/sign-in
Disallow: /health/sign-up
Disallow: /health/forgot-password
Disallow: /health/sso-callback
Disallow: /health/claim-invite
Disallow: /health/admin-redirect
Disallow: /health/plans/
```

A trailing slash is load-bearing: `/health/plans/` blocks the plan detail pages while leaving `/health/plans` itself indexable.
